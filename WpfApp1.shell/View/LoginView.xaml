<Window x:Class="WpfApp1.shell.View.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Height="600" Width="900" MinWidth="900" MinHeight="600" MaxWidth="900" MaxHeight="600"
        Title="Вход" WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <!-- Стиль для закругленных кнопок -->
        <Style x:Key="RoundedButton" TargetType="Button">
            <Setter Property="Background" Value="#D9D9D9"/>
            <Setter Property="Foreground" Value="#7685E5"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="10"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Black" BlurRadius="4" ShadowDepth="2" Opacity="0.2"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E0E0E0"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#C0C0C0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <!-- Фоновое изображение -->
        <Image Source="/View/фон.png" Stretch="UniformToFill"/>

        <!-- Затемняющий слой -->
        <Rectangle Fill="Black" Opacity="0.1"/>

        <!-- Основной контент с тенью -->
        <Border Background="#7685E5" 
            CornerRadius="10" 
            Padding="30" 
            HorizontalAlignment="Center" 
            VerticalAlignment="Center" 
            Width="250">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" Opacity="0.5" ShadowDepth="0"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="Логин:" Foreground="White" Margin="0,0,0,8" FontSize="20" FontWeight="SemiBold"/>
                <Border CornerRadius="5" Background="White" Margin="0,0,0,15">
                    <TextBox Text="{Binding Username}" 
                         Background="Transparent" 
                         BorderThickness="0"
                         FontSize="18" 
                         Padding="8"
                         Foreground="#38427C"
                             Height="40"/>
                </Border>

                <TextBlock Text="Пароль:" Foreground="White" Margin="0,0,0,8" FontSize="20" FontWeight="SemiBold"/>
                <Border CornerRadius="5" Background="White" Margin="0,0,0,20">
                    <PasswordBox x:Name="PasswordBox" 
                             Background="Transparent" 
                             BorderThickness="0"
                             FontSize="18" 
                             Padding="8"
                             Height="40"/>
                </Border>

                <Button Content="Войти" 
                    Command="{Binding LoginCommand}" 
                    Style="{StaticResource RoundedButton}" 
                    Background="White"
                    Foreground="#5E6BBD"
                    Click="LoginButton_Click" Height="40"
                    FontSize="20"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>